# Eddy is now authenticated via the local `claude` CLI subscription —
# no API key is required. Settings below are optional overrides.

# Port the Express server listens on.
# PORT=4317

# Port Vite's dev proxy forwards /api to (matches PORT above).
# EDDY_BACKEND_PORT=4317
